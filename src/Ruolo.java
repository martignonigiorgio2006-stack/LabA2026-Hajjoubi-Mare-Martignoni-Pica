/**
 * Rappresenta i possibili tipi di ruolo di un {@code Utente}.
 * 
 */
public enum Ruolo {
    /** Ruolo cliente. */
    CLIENTE,
    /** Ruolo bigliettaio. */
    BIGLIETTAIO,
    /** Ruolo proiezionista. */
    PROIEZIONISTA
}
